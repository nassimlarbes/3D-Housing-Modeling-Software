/*
 * Click nbfs://nbhost/SystemFileSystem/Templates/Licenses/license-default.txt to change this license
 * Click nbfs://nbhost/SystemFileSystem/Templates/Classes/Class.java to edit this template
 */
package Display;

import java.awt.Color;
import java.awt.Graphics;
import java.awt.Polygon;
import java.util.ArrayList;

public class ChaletDrawer {
   /* public void drawer(Graphics g, ArrayList<PolygonClass> listPolygon){
        for (PolygonClass item: listPolygon) {
            if (item != null){
                Polygon newPoly = new Polygon();
                for (Position position : item.getListePointImperiaux()){
                    //convertion en pixel
                    int x = position.getX().convertToPixels();
                    int y = position.getY().convertToPixels();
                    newPoly.addPoint(x,y) ; // Check hiba and ouissal
                    
                }
                g.setColor(Color.GRAY);
                g.drawPolygon(newPoly);
                g.setColor(item.getcolor());
                int xPoly = newPoly.xpoints[0];
                int yPoly = newPoly.ypoints[0];
                newPoly.addPoint(xPoly, yPoly);
            }
        }
    }*/
    
}
